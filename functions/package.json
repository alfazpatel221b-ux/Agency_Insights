{
  "name": "functions",
  "description": "Firestore actionItems → Google Sheets plus email/team alerts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^6.6.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "firebase-functions-test": "^3.5.0",
    "typescript": "^5.9.3"
  },
  "private": true
}
